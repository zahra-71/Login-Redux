import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  // toollbar: theme.mixins.toolbar,
  container: {
    marginTop: theme.spacing(10),
    marginRight: theme.spacing(5),
    // minWidth: "100vw",
  }
}))